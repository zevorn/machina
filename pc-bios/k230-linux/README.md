# K230 Linux Boot Artifacts

These files are copied from:

```text
~/k230_sdk/output/k230_canmv_defconfig/images/little-core/
```

They are used by the K230 mtest regression to cover direct Linux boot with the
built-in SBI path:

- `Image`
- `k230.dtb`
- `rootfs.cpio.gz`

[package]
name = "machina-frontend"
version = "0.1.0"
edition = "2021"

[dependencies]
machina-accel = { path = "../accel" }
libc = "0.2"

[build-dependencies]
machina-decode = { path = "../decode" }

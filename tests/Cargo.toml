[package]
name = "machina-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
machina-accel = { path = "../accel" }
machina-frontend = { path = "../frontend" }
machina-decode = { path = "../decode" }
libc = "0.2"
